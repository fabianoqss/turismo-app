services:
  backend:
    build: 
      context: ./backend/
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    # networks:
    #   - app_network
    restart: unless-stopped

# services:
#   nginx:
#     image: nginx:alpine
#     ports:
#       - "80:80"          # only nginx is exposed to the outside
#     volumes:
#       - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
#     depends_on:
#       - backend
#     networks:
#       - app_network
#
#   backend:
#     build:
#       context: ./backend/
#       dockerfile: Dockerfile
#     # NO ports exposed here — backend is internal only
#     restart: unless-stopped
#     networks:
#       - app_network
#
# networks:
#   app_network:
#     driver: bridge
